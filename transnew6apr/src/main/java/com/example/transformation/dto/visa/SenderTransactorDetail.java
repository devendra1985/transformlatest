/*
 * Visa Direct Account and Wallet
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.example.transformation.dto.visa;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.example.transformation.dto.visa.SenderCompany;
import com.example.transformation.dto.visa.SenderIndividual;
import com.example.transformation.dto.visa.Type;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.time.LocalDate;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import com.example.transformation.dto.JSON;

@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-02-10T20:13:57.632250+05:30[Asia/Kolkata]", comments = "Generator version: 7.7.0")
@JsonDeserialize(using = SenderTransactorDetail.SenderTransactorDetailDeserializer.class)
@JsonSerialize(using = SenderTransactorDetail.SenderTransactorDetailSerializer.class)
public class SenderTransactorDetail extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(SenderTransactorDetail.class.getName());

    public static class SenderTransactorDetailSerializer extends StdSerializer<SenderTransactorDetail> {
        public SenderTransactorDetailSerializer(Class<SenderTransactorDetail> t) {
            super(t);
        }

        public SenderTransactorDetailSerializer() {
            this(null);
        }

        @Override
        public void serialize(SenderTransactorDetail value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class SenderTransactorDetailDeserializer extends StdDeserializer<SenderTransactorDetail> {
        public SenderTransactorDetailDeserializer() {
            this(SenderTransactorDetail.class);
        }

        public SenderTransactorDetailDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public SenderTransactorDetail deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize SenderCompany
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (SenderCompany.class.equals(Integer.class) || SenderCompany.class.equals(Long.class) || SenderCompany.class.equals(Float.class) || SenderCompany.class.equals(Double.class) || SenderCompany.class.equals(Boolean.class) || SenderCompany.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((SenderCompany.class.equals(Integer.class) || SenderCompany.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((SenderCompany.class.equals(Float.class) || SenderCompany.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (SenderCompany.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (SenderCompany.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(SenderCompany.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'SenderCompany'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'SenderCompany'", e);
            }

            // deserialize SenderIndividual
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (SenderIndividual.class.equals(Integer.class) || SenderIndividual.class.equals(Long.class) || SenderIndividual.class.equals(Float.class) || SenderIndividual.class.equals(Double.class) || SenderIndividual.class.equals(Boolean.class) || SenderIndividual.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((SenderIndividual.class.equals(Integer.class) || SenderIndividual.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((SenderIndividual.class.equals(Float.class) || SenderIndividual.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (SenderIndividual.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (SenderIndividual.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(SenderIndividual.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'SenderIndividual'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'SenderIndividual'", e);
            }

            if (match == 1) {
                SenderTransactorDetail ret = new SenderTransactorDetail();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format("Failed deserialization for SenderTransactorDetail: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public SenderTransactorDetail getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "SenderTransactorDetail cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public SenderTransactorDetail() {
        super("oneOf", Boolean.FALSE);
    }

    public SenderTransactorDetail(SenderCompany o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public SenderTransactorDetail(SenderIndividual o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("SenderCompany", SenderCompany.class);
        schemas.put("SenderIndividual", SenderIndividual.class);
        JSON.registerDescendants(SenderTransactorDetail.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return SenderTransactorDetail.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * SenderCompany, SenderIndividual
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(SenderCompany.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(SenderIndividual.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be SenderCompany, SenderIndividual");
    }

    /**
     * Get the actual instance, which can be the following:
     * SenderCompany, SenderIndividual
     *
     * @return The actual instance (SenderCompany, SenderIndividual)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `SenderCompany`. If the actual instance is not `SenderCompany`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SenderCompany`
     * @throws ClassCastException if the instance is not `SenderCompany`
     */
    public SenderCompany getSenderCompany() throws ClassCastException {
        return (SenderCompany)super.getActualInstance();
    }

    /**
     * Get the actual instance of `SenderIndividual`. If the actual instance is not `SenderIndividual`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SenderIndividual`
     * @throws ClassCastException if the instance is not `SenderIndividual`
     */
    public SenderIndividual getSenderIndividual() throws ClassCastException {
        return (SenderIndividual)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof SenderIndividual) {
        if (getActualInstance() != null) {
          joiner.add(((SenderIndividual)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof SenderCompany) {
        if (getActualInstance() != null) {
          joiner.add(((SenderCompany)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

