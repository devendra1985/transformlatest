/*
 * Visa Direct Account and Wallet
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.example.transformation.dto.visa;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.example.transformation.dto.visa.SenderCompany1;
import com.example.transformation.dto.visa.SenderIndividual1;
import com.example.transformation.dto.visa.Type;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.time.LocalDate;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import com.example.transformation.dto.JSON;

@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-02-10T20:13:57.632250+05:30[Asia/Kolkata]", comments = "Generator version: 7.7.0")
@JsonDeserialize(using = SenderTransactorDetail1.SenderTransactorDetail1Deserializer.class)
@JsonSerialize(using = SenderTransactorDetail1.SenderTransactorDetail1Serializer.class)
public class SenderTransactorDetail1 extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(SenderTransactorDetail1.class.getName());

    public static class SenderTransactorDetail1Serializer extends StdSerializer<SenderTransactorDetail1> {
        public SenderTransactorDetail1Serializer(Class<SenderTransactorDetail1> t) {
            super(t);
        }

        public SenderTransactorDetail1Serializer() {
            this(null);
        }

        @Override
        public void serialize(SenderTransactorDetail1 value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class SenderTransactorDetail1Deserializer extends StdDeserializer<SenderTransactorDetail1> {
        public SenderTransactorDetail1Deserializer() {
            this(SenderTransactorDetail1.class);
        }

        public SenderTransactorDetail1Deserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public SenderTransactorDetail1 deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize SenderCompany1
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (SenderCompany1.class.equals(Integer.class) || SenderCompany1.class.equals(Long.class) || SenderCompany1.class.equals(Float.class) || SenderCompany1.class.equals(Double.class) || SenderCompany1.class.equals(Boolean.class) || SenderCompany1.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((SenderCompany1.class.equals(Integer.class) || SenderCompany1.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((SenderCompany1.class.equals(Float.class) || SenderCompany1.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (SenderCompany1.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (SenderCompany1.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(SenderCompany1.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'SenderCompany1'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'SenderCompany1'", e);
            }

            // deserialize SenderIndividual1
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (SenderIndividual1.class.equals(Integer.class) || SenderIndividual1.class.equals(Long.class) || SenderIndividual1.class.equals(Float.class) || SenderIndividual1.class.equals(Double.class) || SenderIndividual1.class.equals(Boolean.class) || SenderIndividual1.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((SenderIndividual1.class.equals(Integer.class) || SenderIndividual1.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((SenderIndividual1.class.equals(Float.class) || SenderIndividual1.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (SenderIndividual1.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (SenderIndividual1.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(SenderIndividual1.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'SenderIndividual1'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'SenderIndividual1'", e);
            }

            if (match == 1) {
                SenderTransactorDetail1 ret = new SenderTransactorDetail1();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format("Failed deserialization for SenderTransactorDetail1: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public SenderTransactorDetail1 getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "SenderTransactorDetail1 cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public SenderTransactorDetail1() {
        super("oneOf", Boolean.FALSE);
    }

    public SenderTransactorDetail1(SenderCompany1 o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public SenderTransactorDetail1(SenderIndividual1 o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("SenderCompany1", SenderCompany1.class);
        schemas.put("SenderIndividual1", SenderIndividual1.class);
        JSON.registerDescendants(SenderTransactorDetail1.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return SenderTransactorDetail1.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * SenderCompany1, SenderIndividual1
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(SenderCompany1.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(SenderIndividual1.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be SenderCompany1, SenderIndividual1");
    }

    /**
     * Get the actual instance, which can be the following:
     * SenderCompany1, SenderIndividual1
     *
     * @return The actual instance (SenderCompany1, SenderIndividual1)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `SenderCompany1`. If the actual instance is not `SenderCompany1`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SenderCompany1`
     * @throws ClassCastException if the instance is not `SenderCompany1`
     */
    public SenderCompany1 getSenderCompany1() throws ClassCastException {
        return (SenderCompany1)super.getActualInstance();
    }

    /**
     * Get the actual instance of `SenderIndividual1`. If the actual instance is not `SenderIndividual1`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SenderIndividual1`
     * @throws ClassCastException if the instance is not `SenderIndividual1`
     */
    public SenderIndividual1 getSenderIndividual1() throws ClassCastException {
        return (SenderIndividual1)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof SenderIndividual1) {
        if (getActualInstance() != null) {
          joiner.add(((SenderIndividual1)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof SenderCompany1) {
        if (getActualInstance() != null) {
          joiner.add(((SenderCompany1)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

